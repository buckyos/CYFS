{
	"name": "ood-daemon",
	"version": "1.0.0",
	"scripts": {
		"install": "\"{root}/bin/ood-daemon\" --install",
		"start": "\"{root}/bin/ood-daemon\" --start",
		"stop": "\"{root}/bin/ood-daemon\" --stop --no-monitor",
		"status": "\"{root}/bin/ood-daemon\" --status"
	}
}
